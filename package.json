{
  "name": "chunking-tool",
  "version": "2.3.0",
  "private": true,
  "description": "Chunking Tool — Chrome extension for splitting text into sized chunks.",
  "scripts": {
    "test": "node --test tests/test-core.js"
  }
}
