{
  "name": "cect-c-chunking-tool",
  "version": "2.3.0",
  "private": true,
  "description": "CECT-C Chunking Tool — Chrome extension for splitting text into sized chunks.",
  "scripts": {
    "test": "node --test tests/test-core.js"
  }
}
